% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Mobius_vMF.R
\name{estprep_meanconstraints}
\alias{estprep_meanconstraints}
\title{Prepare mean-link constraint setup for optimisation}
\usage{
estprep_meanconstraints(om0, fix_qs1, fix_qe1)
}
\arguments{
\item{om0}{Starting \code{\link{mobius_link_Omega}} object.}

\item{fix_qs1}{Logical; whether to hold the first column of \code{Qs} fixed.}

\item{fix_qe1}{Logical; whether to hold \code{ce} and the first column of \code{Qe} fixed.}
}
\value{
A list:
\describe{
\item{om0vec}{Starting parameters as a flat vector: \code{c(p1, qs1, qe1, Omega, ce)}.}
\item{isfixed}{Logical vector marking fixed positions in \code{om0vec}.}
\item{x0}{Starting values for the free (non-fixed) parameters, possibly perturbed to
avoid a singular constraint Jacobian.}
\item{constraint_tape}{AD tape evaluating orthonormality constraints on \code{P}, \code{Qs}, \code{Qe}
and their Jacobians, with fixed-parameter dimensions already reduced.}
}
}
\description{
Prepares the constraint tape, fixed-parameter mask, and starting point for constrained
optimisation of the Mobius mean link parameters in \code{\link[=mobius_vMF]{mobius_vMF()}}.
Specifically, the function:
\enumerate{
\item Vectorises \code{om0} to \code{om0vec}, the starting parameters as \code{c(p1, qs1, qe1, Omega, ce)}.
\item Creates an AD constraint tape evaluating the orthonormality constraints on \code{P}, \code{Qs},
\code{Qe} and their Jacobians.
\item Determines \code{isfixed} — a logical mask of which positions in \code{om0vec} are held constant,
based on \code{fix_qs1} and \code{fix_qe1}.
\item Reduces the tape via \code{fixindependent} and \code{keeprange}, dropping constraint outputs that
become constant once fixed parameters are removed.
\item Computes \code{x0 = om0vec[!isfixed]}, the free-parameter starting vector.
\item Checks the constraint Jacobian at \code{x0} for singularity (near-zero singular values).
\item If singular, perturbs \code{x0} by adding small noise (from a pregenerated matrix in
\code{R/sysdata.rda}) to the off-diagonal columns of \code{Qs} and \code{Qe}, then recomputes \code{x0}.
}
}
\keyword{internal}

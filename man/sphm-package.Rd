% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sphm-package.R
\docType{package}
\name{sphm-package}
\alias{sphm-package}
\alias{sphm}
\title{sphm: Spherical Regression by Scaled Mobius Link}
\description{
Regression for spherical response data and scaled von Mises-Fisher error distribution as described in the manuscript "Regression for spherical responses with linear and spherical covariates using a scaled link function" by Shogo Kato, Kassel L. Hingee, Janice L. Scealy, and Andrew T.A. Wood.
}
\section{Overview}{

\code{sphm} implements regression for data where each observation is a unit vector
(a point on a sphere \eqn{S^{p-1}}). Covariates may be Euclidean vectors,
spherical (unit vector) covariates, or both.

The regression model uses a \strong{scaled Möbius link function} to map covariates
to predicted mean directions, combined with an elliptically symmetric error
distribution. See the manuscript "Regression for spherical responses with
linear and spherical covariates using a scaled link function" (Kato, Hingee,
Scealy, Wood) for full details.
}

\section{Typical workflow}{


\strong{Step 1 — preliminary vMF regression} with \code{\link[=mobius_vMF]{mobius_vMF()}}

Fits a fast preliminary regression using the von Mises–Fisher (vMF) error
distribution. The optimiser maximises \eqn{\sum_i y_i^\top \mu(x_i)},
which is equivalent to maximising the vMF log-likelihood over the mean link
parameters only. The result provides good starting values for Step 2.

\strong{Step 2 — full SvMF regression} with \code{\link[=mobius_SvMF]{mobius_SvMF()}}

Fits the full model using the scaled von Mises–Fisher (SvMF) error
distribution, which allows anisotropic (elliptical) spread around the
predicted mean. All parameters are estimated jointly by maximum likelihood
using automatic differentiation. The axes of symmetry of the error
distribution are constructed via parallel transport (see \code{\link[=parallel_transport_mat]{parallel_transport_mat()}}).

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Example: simulate then fit on S^2 with a spherical covariate
set.seed(1)
n <- 50; p <- 3
xs <- matrix(rnorm(n * p), n, p)
xs <- xs / sqrt(rowSums(xs^2))
mean_params <- rmnlink_cann(p = p, qs = p, qe = 0)
sim <- rMobius_SvMF(xs = xs, xe = NULL, mean = mean_params,
                    k = 5, a = c(1, 2, 0.5), G0 = diag(p))
y <- sim[, 1:p]

fit_vMF  <- mobius_vMF(y = y, xs = xs)           # Step 1
fit_SvMF <- mobius_SvMF(y = y, xs = xs,           # Step 2
                         start = fit_vMF$mean)
}\if{html}{\out{</div>}}
}

\section{Key functions}{
\tabular{ll}{
   Task \tab Function \cr
   \strong{Fit regression} \tab \code{\link[=mobius_SvMF]{mobius_SvMF()}}, \code{\link[=mobius_vMF]{mobius_vMF()}}, \code{\link[=mobius_vMF_restart]{mobius_vMF_restart()}} \cr
   \strong{Simulate data} \tab \code{\link[=rMobius_SvMF]{rMobius_SvMF()}}, \code{\link[=rSvMF]{rSvMF()}} \cr
   \strong{Evaluate link} \tab \code{\link[=mnlink]{mnlink()}} \cr
   \strong{Residuals} \tab \code{\link[=rotatedresid]{rotatedresid()}}, \code{\link[=parallel_transport_mat]{parallel_transport_mat()}} \cr
   \strong{SvMF density} \tab \code{\link[=dSvMF]{dSvMF()}}, \code{\link[=ldMobius_SvMF]{ldMobius_SvMF()}} \cr
   \strong{SvMF prelim. estimates} \tab \code{\link[=SvMF_moment_axes]{SvMF_moment_axes()}}, \code{\link[=SvMF_prelim_scales]{SvMF_prelim_scales()}} \cr
   \strong{Link parameterisations} \tab \code{\link[=mnlink_cann]{mnlink_cann()}}, \code{\link[=mnlink_Omega]{mnlink_Omega()}}, \code{\link[=cann2Omega]{cann2Omega()}} \cr
   \strong{SvMF parameterisations} \tab \code{\link[=SvMFcann]{SvMFcann()}}, \code{\link[=SvMFmuV]{SvMFmuV()}} \cr
   \strong{Data standardisation} \tab \code{\link[=standardise_sph]{standardise_sph()}}, \code{\link[=secondmoment_mat]{secondmoment_mat()}} \cr
   \strong{Degrees of freedom} \tab \code{\link[=mobius_DoF]{mobius_DoF()}} \cr
}
}

\section{Link function parameterisations}{


The mean link has two equivalent parameterisations:
\itemize{
\item \strong{Canonical} (\code{\link[=mnlink_cann]{mnlink_cann()}}): parameters \code{P}, \code{Bs}, \code{Qs}, \code{Be}, \code{Qe}
directly match the notation in the manuscript (Equation 1). Use this for
interpreting fitted results.
\item \strong{Omega} (\code{\link[=mnlink_Omega]{mnlink_Omega()}}): a reparameterisation that merges the rotation
and scale matrices into a single matrix \code{Omega}. Used internally during
optimisation to avoid working on Stiefel manifolds.
}

Convert between them with \code{\link[=as_mnlink_cann]{as_mnlink_cann()}}, \code{\link[=as_mnlink_Omega]{as_mnlink_Omega()}}, and
\code{\link[=cann2Omega]{cann2Omega()}}.
}

\section{C++ and automatic differentiation}{

The log-likelihood (and its gradient) are implemented
in C++ using \code{Eigen} and \code{CppAD} (accessed via the \code{scorematchingad} package) to allow for complicated gradient-based optimisation.
The C++ code is in \verb{src/}.
}

\section{Reproducing manuscript results}{

Two Quarto documents in \verb{vignettes/} reproduce all numerical results from the
manuscript:
\itemize{
\item \code{reproduce_midatlantic.qmd} — midatlantic ridge data on \eqn{S^2}
\item \code{reproduce_earthquakes.qmd} — earthquake moment tensors on \eqn{S^4}
}
}

\author{
\strong{Maintainer}: Kassel Liam Hingee \email{kassel.hingee@anu.edu.au} (\href{https://orcid.org/0000-0001-9894-2407}{ORCID})

}
\keyword{internal}

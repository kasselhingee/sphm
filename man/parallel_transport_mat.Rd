% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotatedresiduals.R
\name{parallel_transport_mat}
\alias{parallel_transport_mat}
\title{Parallel transport matrix}
\usage{
parallel_transport_mat(start, end)
}
\arguments{
\item{start}{Starting location as a unit vector.}

\item{end}{End location as a unit vector.}
}
\value{
A square orthogonal matrix.
}
\description{
Rotation matrix that performs parallel transport along the minimum-distance geodesic
on the sphere from \code{start} to \code{end}. When applied to a tangent vector at \code{start}, it moves
that vector to the corresponding tangent vector at \code{end}. When applied to \code{start} itself,
it returns \code{end}.

This is the matrix from Amaral et al. (2007, Lemma 2), which is equivalent to the negative
of Jupp's (1988) rotation matrix (internal function \code{JuppRmat()}). Both perform the same
parallel transport; this version uses a rotation-based formula.
}
\details{
If \code{end = -start} (antipodal points), there is no unique geodesic and a reflection
matrix in the \code{end} direction is returned.
}
\seealso{
\code{\link[=rotated_resid]{rotated_resid()}}

Other residuals: 
\code{\link{rotated_resid}()}
}
\concept{residuals}

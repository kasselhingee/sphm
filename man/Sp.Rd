% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{Sp}
\alias{Sp}
\alias{iSp}
\title{Stereographic projection from the south pole}
\usage{
Sp(x)

iSp(y)
}
\arguments{
\item{x}{A matrix of row vectors on the sphere \eqn{S^{p-1}}, or a single such vector.}

\item{y}{A matrix of row vectors in \eqn{R^{p-1}}, or a single such vector.}
}
\value{
A matrix with the same number of rows as the input: \code{p-1} columns for \code{Sp()}
and \code{p} columns for \code{iSp()}. If the input is a single vector (or a matrix with one
row) the result is returned as a plain vector rather than a one-row matrix.
}
\description{
\code{Sp()} is the stereographic projection from the south pole
\eqn{-e_1 = (-1, 0, \ldots, 0)} of \eqn{S^{p-1}} to \eqn{R^{p-1}}: for
\eqn{x = (x_1, \ldots, x_p)} on the sphere,
\deqn{Sp(x)_j = x_{j+1} / (1 + x_1), \quad j = 1, \ldots, p-1.}
\code{iSp()} is its inverse.

This is the projection used by the scaled Mobius link \code{\link[=mobius_link]{mobius_link()}}, which applies
\code{Sp()} to the spherical covariates, accumulates the scaled displacements linearly in
\eqn{R^{p-1}}, and maps the result back to the sphere with \code{iSp()}.

\code{iSp()} maps \eqn{y} in \eqn{R^{p-1}} back to \eqn{S^{p-1}} via
\deqn{iSp(y) = (1 - \|y\|^2, 2y) / (1 + \|y\|^2).}
It is defined everywhere on \eqn{R^{p-1}} and never returns the south pole.
}
\details{
\code{Sp()} is undefined at the projection point itself, the south pole
\eqn{-e_1}. Rows of \code{x} equal to \eqn{-e_1} are returned as \code{1e9} rather than
\code{Inf}, so that downstream optimisation sees a large finite number.
}
\examples{
x <- rbind(north_pole(3), c(0, 1, 0), c(0, 0, 1))
Sp(x)
iSp(Sp(x))  # recovers x

Sp(north_pole(3))          # the north pole maps to the origin
Sp(c(-1, 0, 0))            # the south pole: undefined, returned as 1e9
}
\seealso{
Other link-function:
\code{\link[=mobius_link]{mobius_link()}},
\code{\link{mobius_link_params}},
\code{\link[=mobius_link_prop4i]{mobius_link_prop4i()}},
\code{\link[=mobius_link_prop4ii]{mobius_link_prop4ii()}},
\code{\link[=rand_mobius_link_cann]{rand_mobius_link_cann()}}
}
\concept{link-function}

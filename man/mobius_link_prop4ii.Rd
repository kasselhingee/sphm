% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Mobius_prop4ii.R
\name{mobius_link_prop4ii}
\alias{mobius_link_prop4ii}
\alias{as_mobius_link_prop4ii}
\title{Proposition 4(ii) Mean Link Parameters}
\usage{
mobius_link_prop4ii(
  Rtilde0,
  P = NULL,
  Qe_star = NULL,
  Be = NULL,
  xe_center = NULL,
  xe_scale = NULL,
  intercept = TRUE,
  check = TRUE
)

as_mobius_link_prop4ii(obj, check = TRUE)
}
\arguments{
\item{Rtilde0}{A \code{p} x \code{p} orthonormal matrix.}

\item{P}{Final rotation on the response sphere: a \code{p} x \code{p} rotation matrix. Supplied
only when the fit has Euclidean covariates, in which case \eqn{\tilde R_0 = P Q_s^\top}.}

\item{Qe_star}{An \code{m} x \code{(p-1)} matrix with orthonormal columns acting on the
standardised Euclidean covariates, where \code{m} counts them plus any intercept.}

\item{Be}{The \code{p-1} diagonal entries of the Euclidean scaling matrix, each in \verb{(0, 1)}.}

\item{xe_center, xe_scale}{The centre and scale used to standardise the Euclidean
covariates during fitting, so that predictions reproduce them.}

\item{intercept}{\code{TRUE} if an intercept column was appended to the Euclidean covariates.}

\item{check}{Whether to validate the result.}

\item{obj}{An object to coerce or validate.}
}
\value{
\code{mobius_link_prop4ii()} and \code{as_mobius_link_prop4ii()} return an object of class
"mobius_link_prop4ii". \code{dim()} returns the named vector \code{c(p, qs, qe)}.
}
\description{
Parameters of the scaled Mobius mean link constrained by
\eqn{p = q_s} and \eqn{B_s = I_{p-1}} --- Proposition 4(ii) of the manuscript.
Without Euclidean covariates the link is simply \eqn{\mu(x_s) = \tilde R_0 x_s}.
}
\details{
\code{P}, \code{Qe_star} and \code{Be} must be supplied together or not at all. When they are supplied
the link is \eqn{\mu(x_s, x_e) = P\,\mathcal{S}^{[-1]}\{\mathcal{S}(Q_s^\top x_s) +
B_e Q_e^{*\top} \tilde x_e\}}, matching the \code{LinEuc} construction with \eqn{B_s} held
at the identity.

Use \code{\link[=as_mobius_link_cann]{as_mobius_link_cann()}} to convert to the canonical parameterisation for
interpretation; see \code{\link{mobius_link_params}}.
}
\section{Functions}{
\itemize{
\item \code{as_mobius_link_prop4ii()}: Coerce a compatible object to the Proposition 4(ii)
parameterisation. Accepts a \code{p} x \code{p} matrix, a list with an \code{Rtilde0} element, or a
canonical object with \code{Bs = I} and \code{p = qs}.

}}
\seealso{
\code{\link{mobius_link_prop4i}} for the isotropic-scale case, \code{\link[=mobius_link]{mobius_link()}},
\code{\link[=mobius_SvMF]{mobius_SvMF()}}.

Other link-function:
\code{\link[=Sp]{Sp()}},
\code{\link[=mobius_link]{mobius_link()}},
\code{\link{mobius_link_params}},
\code{\link[=mobius_link_prop4i]{mobius_link_prop4i()}},
\code{\link[=rand_mobius_link_cann]{rand_mobius_link_cann()}}
}
\concept{link-function}

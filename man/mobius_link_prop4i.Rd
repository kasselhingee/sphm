% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Mobius_prop4i.R
\name{mobius_link_prop4i}
\alias{mobius_link_prop4i}
\alias{as_mobius_link_prop4i}
\title{Proposition 4(i) Mean Link Parameters}
\usage{
mobius_link_prop4i(
  Rtilde0,
  psi = NULL,
  beta_s = NULL,
  rs1 = NULL,
  P = NULL,
  Qe_star = NULL,
  Be = NULL,
  xe_center = NULL,
  xe_scale = NULL,
  intercept = TRUE,
  check = TRUE
)

as_mobius_link_prop4i(obj, check = TRUE)
}
\arguments{
\item{Rtilde0}{A \code{p} x \code{p} orthonormal matrix.}

\item{psi}{The Mobius shift, a vector of length \code{p} with \verb{||psi|| < 1}. Supply either
\code{psi}, or \code{beta_s} and \code{rs1}.}

\item{beta_s}{The isotropic spherical scale, in \verb{(0, 1]}.}

\item{rs1}{The reference direction, a unit vector of length \code{p}. Not identified when
\code{beta_s == 1}.}

\item{P}{Final rotation on the response sphere: a \code{p} x \code{p} rotation matrix. Supplied
only when the fit has Euclidean covariates, in which case \eqn{\tilde R_0 = P Q_s^\top}
and \code{rs1}/\code{psi} are derived from it rather than taken from the arguments.}

\item{Qe_star}{An \code{m} x \code{(p-1)} matrix with orthonormal columns acting on the
standardised Euclidean covariates, where \code{m} counts them plus any intercept.}

\item{Be}{The \code{p-1} diagonal entries of the Euclidean scaling matrix, each in \verb{(0, 1)}.}

\item{xe_center, xe_scale}{The centre and scale used to standardise the Euclidean
covariates during fitting, so that predictions reproduce them.}

\item{intercept}{\code{TRUE} if an intercept column was appended to the Euclidean covariates.}

\item{check}{Whether to validate the result.}

\item{obj}{An object to coerce or validate.}
}
\value{
\code{mobius_link_prop4i()} and \code{as_mobius_link_prop4i()} return an object of class
"mobius_link_prop4i". \code{dim()} returns the named vector \code{c(p, qs, qe)}.
}
\description{
Parameters of the scaled Mobius mean link constrained by \eqn{p = q_s} and
an isotropic spherical scale \eqn{B_s = \beta_s I_{p-1}}, \eqn{0 < \beta_s \le 1} ---
Proposition 4(i) of the manuscript. Without Euclidean covariates the link is the
isotropic Mobius map \eqn{\mu_s(x_s) = M_S(x_s : \tilde R_0, \psi)} with
\eqn{\psi = \phi r_{s1}} and \eqn{\phi = (1-\beta_s)/(1+\beta_s)}.
\eqn{\beta_s = 1} gives \code{\link{mobius_link_prop4ii}}.
}
\details{
Proposition 4(i) itself is stated for \eqn{q_e = 0}. Supplying \code{P}, \code{Qe_star} and \code{Be}
(together, or not at all) keeps the restriction on the spherical component and adds the
same linear Euclidean term the \code{LinEuc} link uses, so \eqn{\beta_s = 1} reduces exactly
to the Proposition 4(ii)+Euclidean model.

Use \code{\link[=as_mobius_link_cann]{as_mobius_link_cann()}} to convert to the canonical parameterisation for
interpretation; see \code{\link{mobius_link_params}}.
}
\section{Functions}{
\itemize{
\item \code{as_mobius_link_prop4i()}: Coerce a compatible object to the Proposition 4(i)
parameterisation.

}}
\seealso{
\code{\link{mobius_link_prop4ii}}, \code{\link[=mobius_link]{mobius_link()}}, \code{\link[=mobius_SvMF]{mobius_SvMF()}}.

Other link-function:
\code{\link[=Sp]{Sp()}},
\code{\link[=mobius_link]{mobius_link()}},
\code{\link{mobius_link_params}},
\code{\link[=mobius_link_prop4ii]{mobius_link_prop4ii()}},
\code{\link[=rand_mobius_link_cann]{rand_mobius_link_cann()}}
}
\concept{link-function}
